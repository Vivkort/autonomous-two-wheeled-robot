# Scripts

Helper utilities that aren't part of the ROS 2 graph.

- `lidar_capture.py` / `lidar_view.py` — from the Phase 0 spinning-lidar build: capture serial readings and render a 3D point cloud with Open3D. See `docs/spinning_vl53l1x_lidar_build_guide.md`.

> Drop standalone tools here.
